export default class User {
  constructor(firstname, surname, email, password, level) {
    this.firstname = firstname;
    this.surname = surname;
    this.email = email;
    this.password = password;
    this.level = level;
  }
}
